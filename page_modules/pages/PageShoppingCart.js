import {Selector, t} from 'testcafe'class PageShoppingCart {//Selectors  constructor() {    this.titlePage = Selector('.title');    this.itemCart = Selector('.inventory_item_name')    this.continueBtn = Selector('button[id="checkout"]')    this.continueShoppingBtn = Selector('a[class="btn_secondary"]')  }//Generic methods  async clickContinueBtn() {    await t.click(this.continueBtn())  }  async getPageTitle(title) {    return await this.titlePage.innerText  }  //validate if an item exits in the list  async isItemInTheCart(item) {    const counter = await this.itemCart.count;    let coincidence = false;    for (let i = 0; i < counter; i++) {      const title = await this.itemCart.nth(i).innerText;      if (title.match(item)) {        coincidence = true;      }    }    return coincidence  }  //Get item's name in the shopping cart  async getItemList() {    const counter = await this.itemCart.count;    let itemList = [];    for (let i = 0; i < counter; i++) {      const title = await this.itemCart.nth(i).innerText;      itemList.push(title);    }    return itemList;  }}export default new PageShoppingCart();