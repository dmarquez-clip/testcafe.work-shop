import {Role} from 'testcafe'import {PAGE} from "../page_modules/data/PageConstants";import {CREDENTIALS} from "../page_modules/data/Credentials";import PageLogin from "../page_modules/pages/PageLogin"class roles {  standarUser = Role(PAGE.URL, async t => {    await PageLogin.loginForm(CREDENTIALS.STANDAR_USER.USERNAME,        CREDENTIALS.STANDAR_USER.PASSWORD);  }, {preserveUrl: true});}export default new roles();